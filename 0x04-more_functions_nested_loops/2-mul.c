#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: First integer
 * @b: second integer.
 *
 * Return: The product of the two integers
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
